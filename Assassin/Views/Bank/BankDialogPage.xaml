<Page x:Class="Assassin.Views.Bank.BankDialogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Bank"
      Style="{StaticResource PageStyle}">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="LblDialog" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Height="100" Margin="0,0,0,20" TextWrapping="Wrap" Width="345" />
        <TextBox Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" x:Name="TxtBank" Width="125" Margin="0,0,0,20" TextAlignment="Center" MaxLength="6" PreviewKeyDown="TxtBank_PreviewKeyDown" GotFocus="TxtBank_GotFocus" />
        <Button x:Name="BtnAction" Grid.Column="0" Grid.Row="2" Width="125" Margin="0,0,20,0" Click="BtnAction_Click" IsDefault="True" />
        <Button x:Name="BtnCancel" Grid.Column="1" Grid.Row="2" Width="125" Content="_Cancel" IsCancel="True" Click="BtnCancel_Click" />
    </Grid>
</Page>